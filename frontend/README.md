# FinWise — Flutter Frontend

The Android client for FinWise, the AI-powered 24/7 Virtual Financial Advisor.

See the [root README](../README.md) for the full project overview, tech stack, and setup instructions.

```bash
flutter pub get
flutter run        # requires the backend running — see root README
flutter test       # widget tests
```
